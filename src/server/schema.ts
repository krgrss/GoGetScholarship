/**
 * Runtime schemas / types (placeholder)
 *
 * You can define zod schemas or TypeScript types that mirror your database
 * rows and API contracts here, then re-use them across routes and server utils.
 *
 * Example:
 *   export type ScholarshipRow = {
 *     id: string
 *     name: string
 *     sponsor: string | null
 *     url: string | null
 *     raw_text: string
 *     min_gpa: number | null
 *     country: string | null
 *     fields: string[] | null
 *     metadata: unknown
 *   }
 */
